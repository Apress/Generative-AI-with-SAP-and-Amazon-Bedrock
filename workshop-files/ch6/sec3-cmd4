#Initialize your connection
conn = dbapi.connect(
    address='<changesqlendpoint.hana.trial-us10.hanacloud.ondemand.com>',
    port='443',
    user='<ChangeUser>',
    password='<ChangePassword>',
    encrypt=True, 
    sslValidateCertificate=True
)
#If no errors, print connected
print('connected\n')

